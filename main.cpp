#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <queue>
#include <string.h>
#include <utility>
#include <algorithm>

using namespace std;

//#define debug
#ifdef debug
#include <assert.h>
#else
#define assert(s)
#endif

#define FOR(i, n) FORS((i), 0, (n))
#define FORS(i, a, n) for ((i) = (a); (i) < (n); ++(i))
#define FORI(i, n) FORIS((i), 0, (n))
#define FORIS(i, a, n) for (int (i) = (a); (i) < (n); ++(i))

#define GET(n) scanf("%d", &n);
#define P(n) printf("%s = %d\n", #n, n)
#define PRINT(n) printf("%d\n", n)
#define PESP(a, i, n) printf("%d%c", a, i + 1 == n ? '\n' : ' ')

#define lli long long int

int main () {
}
